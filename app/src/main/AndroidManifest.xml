<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ntubcase.gift">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_giftplanner"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".PlanMultipleActivity"></activity>
        <activity android:name=".MakeGiftMessageActivity" />
        <activity android:name=".MakeGiftVideoActivity" />
        <activity android:name=".MakeGiftPhotoActivity" />
        <activity android:name=".MakeGiftTicketActivity" />
        <activity android:name=".loadingActivity" />
        <activity android:name=".GiftReceivedActivity" />
        <activity android:name=".FriendActivity" />
        <activity android:name=".PlanDetailActivity" />
        <activity android:name=".GiftReceivedDetailActivity" />
        <activity android:name=".GiftDetailActivity" />
        <activity android:name=".MakePlanMultipleActivity" />
        <activity android:name=".MakePlanSingleActivity" />
        <activity android:name=".PlanActivity" />
        <activity android:name=".GiftActivity" />
        <activity
            android:name=".LoginActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar"></activity>

        <provider
            android:name="android.support.v4.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/provider_paths" />
        </provider>
    </application>

</manifest>